<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.sang.mapper.InquiryMapper">
	<select id="selectFaqList" resultType="inquiry">
		select * from inquiry order by in_no desc
	</select>
</mapper>